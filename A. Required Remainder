#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int n,x,y,i,j,t;
	cin>>t;
	while(t--)
	{
		cin>>x>>y>>n;
		i=n/x;
		n%=x;
		if(n<y)
		i--;
		cout<<x*i+y<<endl;
	}
   return 0;
    }
